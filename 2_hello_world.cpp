#include<iostream> //Preprocessor Directive: Includes header file that will be used later on

// Blank Lines, Tabs and Spaces have no effect on the code - they make the program easier to read

using namespace std; // std stands for the standard library

int main(){
	//'main()' is a function
	//Every Program starts with the main function

	cout << "Hello World" << endl;
	//'cout' is called the "Output Screen Object" - writes out characters on the computer screen
	//'<<' is called the "Stream Insertion Operator"
	//'endl' - go to the next line
	
	return 0;
	//Indicates everything above ran fine, so main() function returned 0
}}